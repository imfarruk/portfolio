import { makeStyles } from "@mui/styles";
import { AppColors } from "../color/AppColors";

export const useStyles = makeStyles(() => ({
  header: {
    width: "100%",

    minHeight: "90vh",
    height: "auto",
    // background: AppColors.colors.base1,
  },
}));
